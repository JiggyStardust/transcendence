export async function moveCamera(game) {
  const deltaTime = game.engine.getDeltaTime() / 1000;
  const speed = 1;
  const maxDistance = 6;
  const dir = 1;

  //game.camera.position.x += deltaTime * speed;
  //game.camera.setTarget(BABYLON.Vector3.Zero());
}
